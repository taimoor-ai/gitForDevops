# config for user

to tell who change what when commit the code

set name >  git config --global user.name "taimoor"
set useremail> git config --global user.email "taimooor786786@gmail.com"

git config --global defaultBranch main

git config --listq

# **add  changes  fro commiting**

git add .md

git add ./ all files that are modfied or added

# commit changes made

git commit -m "this is first commit"

# check status

git status

# git commit hsitory

git log

# git rollback commit

git checkout refrencecommitid

# connect local repo to github Repo

git remote add origin https://github.com/taimoor-ai/devopsLearningDemo.git

git remote -v //to verify

git pull origin main --rebase

# push Code to the GitREPO

git push -u  origin main

# BRANCHING

## **for a cureent working branch**

show branch name > git branch

rename > git branch -m branchName

crate > git branch branch_name

swithc > git checkout  branch_name

both create+swicth > git checkout -b branch_name

delete branch > git branch -d branch_name //the current branch most be other you want to delete otherwise errro

### from sepcific branch

> git branch new _branch_name soource_brance_name

# clone a github Repo

git clone repolink
